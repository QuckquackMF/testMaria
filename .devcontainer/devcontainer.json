{
  "name": "MariaDB Devcontainer",
  "build": { "dockerfile": "Dockerfile" },
  "forwardPorts": [3306],
  "remoteUser": "vscode",
  "postStartCommand": "sudo service mariadb start && until sudo mariadb-admin ping; do sleep 2; done && python3 .devcontainer/create_db.py"
}
